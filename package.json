{
  "name": "canvas-lib",
  "private": true,
  "version": "0.1.0",
  "description": "Modern, design-forward canvas library for hierarchies and flows.",
  "packageManager": "pnpm@9.12.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm -r --filter='./packages/*' build",
    "test": "pnpm -r --filter='./packages/*' test",
    "lint": "biome check .",
    "format": "biome format --write .",
    "dev:demo": "pnpm --filter=demo dev",
    "publish:check": "pnpm test && pnpm build && pnpm lint && pnpm --filter=@canvas/core publish --dry-run --no-git-checks && pnpm --filter=@canvas/react publish --dry-run --no-git-checks && pnpm --filter=@canvas/tokens publish --dry-run --no-git-checks"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.0",
    "typescript": "^5.6.0"
  }
}
