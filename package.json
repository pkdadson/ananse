{
  "name": "ananse",
  "private": true,
  "version": "0.1.0",
  "description": "Modern, design-forward library for hierarchies and flows.",
  "packageManager": "pnpm@9.12.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm -r --filter='./packages/*' build",
    "test": "pnpm -r --filter='./packages/*' test",
    "lint": "biome check .",
    "format": "biome format --write .",
    "dev:demo": "pnpm --filter=demo dev",
    "publish:check": "pnpm test && pnpm build && pnpm lint && pnpm --filter=@ananse/core publish --dry-run --no-git-checks && pnpm --filter=@ananse/react publish --dry-run --no-git-checks && pnpm --filter=@ananse/tokens publish --dry-run --no-git-checks",
    "bench": "pnpm --filter=@ananse/core build && node scripts/bench-layout.mjs",
    "doctor": "node scripts/ananse-doctor.mjs",
    "create-app": "node scripts/create-ananse-app.mjs"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.0",
    "typescript": "^5.6.0"
  }
}
